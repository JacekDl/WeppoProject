<!DOCTYPE html>
<html lang="en">

<%- include ('components/head.ejs', {title: "Warzywniak - strona główna" , style: "index.css" }) %>

    <body>
        <% if (locals.logged && locals.role==='admin' ) { %>
            <%- include ('components/adminPanel.ejs', {style: "panel.css" }) %>
                <%} else if (locals.logged) { %>
                    <%- include ('components/userPanel.ejs', {style: "panel.css" }) %>
                        <% } else {%>
                            <%- include ('components/defaultPanel.ejs', {style: "panel.css" }) %>
                                <%} %>

                                    <%- include ('components/header.ejs', {style: "header.css" })%>
                                        <%- include ('components/searchBar.ejs', {style: "searchBar.css" }) %>

                                            <main class="goods__panel">
                                                <% for (let i=0; i<products.length; i++) { %>
                                                    <div class="panel__item good">
                                                        <span class="good__name">
                                                            <%= products[i].name%>
                                                        </span>
                                                        <span class="good__price">
                                                            <%= products[i].price%>
                                                        </span>
                                                        <span class="good__description">
                                                            <%= products[i].description%>
                                                        </span>
                                                        <% if (locals.logged) { %>
                                                            <div class="basket__wrapper">
                                                                <label class="basket__item basket__label"
                                                                    for="basket">Dodaj do koszyka</label>
                                                                <button><a href=<%="/basket/" + products[i].name%>><img
                                                                            class="basket__item basket"
                                                                            src="/images/basket.png"
                                                                            id="basket"></a></button>
                                                            </div>
                                                            <% } %>
                                                    </div>
                                                    <% } %>
                                            </main>

                                            <%- include ('components/footer.ejs', {style: "footer.css" }) %>
    </body>

</html>